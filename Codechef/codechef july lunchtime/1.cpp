#include <iostream>
#include <cstring>
#include <cstdio>

#define gc getchar_unlocked
int value (char a[]) {

}
int main()
{
    long long int a[5000];
    int n, c;
    register int i = 0, l = 1;
    char b = gc();
    for (int i = 0; i < n; i++)
    c = b;
    b = gc();
    l++;
    while (b != '\n')
    {
        c = c * (l++);
        c = c + b - 48;
        printf("%d", c);
    }

}
